<UserControl x:Class="Storage.View.AddedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Background="#7F000000"></Border>
        <Button Name="BtnClose" Height="30" Width="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0" Click="BtnClose_Click" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}">
            <Image Source="/Storage;component/Assets/cancel.png" OpacityMask="#4CF3F3F3"></Image>
                </Button>
        <Border Height="Auto" Width="Auto" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" Padding="20">
            <StackPanel Name="StackPanel">
                
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
